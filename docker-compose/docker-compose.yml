version: '3.4'

networks:
  coreconnect:
    name: coreconnect-network
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
          gateway: 172.28.0.1

services:
  coreconnect:
    image: coreconnect:latest
    container_name: coreconnect
    volumes:
      - /var/www/coreconnect:/app/AppData
    restart: unless-stopped
    ports:
      - "5000:5000"
    networks:
      - coreconnect
    environment:
      # ASPNETCORE environment variables should *not* be changed.
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_HTTP_PORTS=5000

      # Other ASP.NET Core configurations can be overridden here, such as Logging.
      # See https://learn.microsoft.com/en-us/aspnet/core/fundamentals/configuration/?view=aspnetcore-8.0
      - Serilog__MinimumLevel__Override__Microsoft.AspNetCore=Warning
      - Serilog__MinimumLevel__Override__System=Warning

      # Values for DbProvider are SQLite, SQLServer, and PostgreSQL.
      - CoreConnect_ApplicationOptions__DbProvider=SQLite
      # The Docker gateway will be used in Forwarded Headers, which is necessary to
      # to correctly determine request hostname and scheme within the app.  If you change
      # the network config, you must update this value as well.
      - CoreConnect_ApplicationOptions__DockerGateway=172.28.0.1
      # This path shouldn't be changed.  It points to the Docker volume.
      - CoreConnect_ConnectionStrings__SQLite=Data Source=/app/AppData/CoreConnect.db
      # If using SQL Server, change the connection string to point to your SQL Server instance.
      - CoreConnect_ConnectionStrings__SQLServer=Server=(localdb)\\mssqllocaldb;Database=CoreConnect-Server-53bc9b9d-9d6a-45d4-8429-2a2761773502;Trusted_Connection=True;MultipleActiveResultSets=true
      # If using PostgreSQL, change the connection string to point to your PostgreSQL instance.
      - CoreConnect_ConnectionStrings__PostgreSQL=Host=localhost;Database=CoreConnect;Username=postgres;Password=changeme;
